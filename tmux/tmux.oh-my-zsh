# Configure tmux plugin
if [[ -n "$ITERM_PROFILE" && -z "$Apple_PubSub_Socket_Render" ]]; then
  export ZSH_TMUX_AUTOSTART=true
  export ZSH_TMUX_AUTOSTART_ONCE=true
  export ZSH_TMUX_AUTOCONNECT=true  
  export ZSH_TMUX_AUTOQUIT=true
fi

plugins+=tmux
